[alembic]
# DB URL is never stored here: provided via -x url=..., env DATABASE_URL,
# or core.config.settings at runtime (see migrations/env.py).
script_location = migrations
prepend_sys_path = .
path_separator = os
version_path_separator = os
